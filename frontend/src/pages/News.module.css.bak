/* News Page Styles - Modern & Responsive */
.container {
  max-width: 1280px;
  margin: 0 auto;
  padding: 0 1.5rem;
  animation: fadeIn 0.4s ease-out;
}

@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

/* Hero Section */
.heroSection {
  margin-bottom: 3rem;
  background: linear-gradient(135deg, #1e3a8a 0%, #3b82f6 100%);
  color: white;
  padding: 4rem 2.5rem;
  border-radius: 24px;
  margin-top: 2rem;
  box-shadow: 0 20px 60px rgba(30, 58, 138, 0.2);
  position: relative;
  overflow: hidden;
  animation: slideDown 0.5s ease-out;
}

@keyframes slideDown {
  from {
    opacity: 0;
    transform: translateY(-20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.heroSection::before {
  content: '';
  position: absolute;
  inset: 0;
  background: radial-gradient(circle at top right, rgba(255, 255, 255, 0.1) 0%, transparent 60%);
  pointer-events: none;
}

.pageTitle {
  font-size: clamp(2rem, 5vw, 3rem);
  font-weight: 800;
  margin-bottom: 0.75rem;
  text-align: center;
  color: white;
  letter-spacing: -0.02em;
  animation: fadeInUp 0.6s ease-out 0.1s both;
}

@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(15px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

  animation: fadeInUp 0.6s ease-out 0.3s both;
}

@media (max-width: 968px) {
  .featuredGrid {
    grid-template-columns: 1fr;
  }
}

.featuredCard {
  position: relative;
  border-radius: 20px;
  overflow: hidden;
  height: 320px;
  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.15);
  transition: transform 0.3s ease, box-shadow 0.3s ease;
  cursor: pointer;
}

.featuredCard:hover {
  transform: translateY(-4px);
  box-shadow: 0 20px 50px rgba(0, 0, 0, 0.25);
}

.featuredCard.mainFeatured {
  height: 420px;
}

@media (max-width: 968px) {
  .featuredCard,
  .featuredCard.mainFeatured {
    height: 300px;
  }
  margin-top: 2rem;
}

@media (max-width: 768px) {
  .featuredGrid {
    grid-template-columns: 1fr;
  }
}

.featuredCard {
  position: relative;
  border-radius: 12px;
  overflow: hidden;
  height: 300px;
  transition: transform 0.6s ease;
}

.featuredCard:hover .featuredImage {
  transform: scale(1.08);
}

.featuredOverlay {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  background: linear-gradient(to top, rgba(0, 0, 0, 0.9) 0%, rgba(0, 0, 0, 0.6) 50%, transparent 100%);
  padding: 2rem 1.75rem 1.7;
  height: 100%;
}

.featuredImage {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.featuredOverlay {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  background: linear-gradient(transparent, rgba(0, 0, 0, 0.8));
  padding: 2relinear-gradient(135deg, #f97316 0%, #ea580c 100%);
  color: white;
  padding: 0.375rem 0.875rem;
  border-radius: 24px;
  font-size: 0.8125rem;
  font-weight: 700;
  margin-bottom: 1rem;
  display: inline-block;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  box-shadow: 0 4px 12px rgba(249, 115, 22, 0.3);
}

.featuredTitle {
  font-size: clamp(1.25rem, 3vw, 1.625rem);
  font-weight: 800;
  margin-bottom: 0.75rem;
  line-height: 1.25;
  letter-spacing: -0.01em;
}

.mainFeatured .featuredTitle {
  font-size: clamp(1.5rem, 4vw, 2rem);
}

.featuredTitleLink {
  color: white;
  text-decoration: none;
  transition: all 0.3s ease;
  display: inline-block;
}

.featuredTitleLink:hover {
  text-shadow: 0 2px 12px rgba(255, 255, 255, 0.3);
  transform: translateX(2px);
}

.featuredExcerpt {
  font-size: clamp(0.875rem, 1.5vw, 1rem);
  line-height: 1.6;
  opacity: 0.95;
  margin-bottom: 0.5rem;
  font-size: 0.95rem;
  line-height: 1.5;
  opacity: 0.9;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  line-clamp: 2;
  -webkit-box-orient: vertical;
  padding: 2rem;
  background: white;
  border-radius: 20px;
  border: 1px solid #e5e7eb;
  box-shadow: 0 4px 20px rgba(0, 0, 0, 0.04);
  animation: fadeInUp 0.5s ease-out 0.2s both;
}

.filtersContainer {
  display: flex;
  flex-direction: column;
  gap: 1.25rem;
}

@media (min-width: 768px) {
  .filtersContainer {
    flex-direction: row;
    align-items: center;
    justify-content: space-between;
  }
}

.searchContainer {
  flex: 1;
  max-width: 480px;
  position: relative;
}

.searchInput {
  width: 100%;
  padding: 0.875rem 1.125rem;
  padding-left: 1.125rem;
  border: 2px solid #e5e7eb;
  border-radius: 12px;
  font-size: 1rem;
  transition: all 0.3s ease;
  background: #f9fafb;
}

.searchInput:hover {
  border-color: #cbd5e1;
  background: white;
}

.searchInput:focus {
  outline: none;
  border-color: #3b82f6;
  box-shadow: 0 0 0 4px rgba(59, 130, 246, 0.1);
  background: white;
  transform: translateY(-1px
  padding: 0.75rem 1rem;
  border: 2px solid #e2e8f0;
  border-radius: 8px;
  font-size: 75rem 1.125rem;
  border: 2px solid #e5e7eb;
  border-radius: 12px;
  background: #f9fafb;
  font-size: 0.9375rem;
  font-weight: 500;
  cursor: pointer;
  transition: all 0.3s ease;
  min-width: 160px;
}

.categorySelect:hover {
  border-color: #cbd5e1;
  background: white;
}

.categorySelect:focus {
  outline: none;
  border-color: #3b82f6;
  box-shadow: 0 0 0 4px rgba(59, 130, 246, 0.1);
  background: white;
}

.featuredFilter {
  display: flex;
  align-items: center;
  gap: 0.625rem;
  font-size: 0.9375rem;
  font-weight: 500;
  cursor: pointer;
  padding: 0.75rem 1.125rem;
  background: #f9fafb;
  border-radius: 12px;
  border: 2px solid #e5e7eb;
  transition: all 0.3s ease;
}

.featuredFilter:hover {
  background: white;
  border-color: #cbd5e1;
}

.featuredFilter input[type="checkbox"] {
  width: 18px;
  height: 18px;
  accent-color: #3b82f6;
  cursor: pointer;
}

.clearFiltersBtn {
  padding: 0.75re4rem;
  animation: fadeIn 0.5s ease-out 0.3s both;
}

.articlesGrid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(340px, 1fr));
  gap: 2rem;
  margin-bottom: 2.5rem;
}

@media (max-width: 1024px) {
  .articlesGrid {
    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
    gap: 1.5rem;
  }
}

@media (max-width: 768px) {
  .articlesGrid {20px;
  overflow: hidden;
  box-shadow: 0 4px 20px rgba(0, 0, 0, 0.06);
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  border: 1px solid #e5e7eb;
  height: 100%;
  display: flex;
  flex-direction: column;
  animation: fadeInUp 0.5s ease-out both;
}

.newsCard:hover {
  transform: translateY(-8px);
  box-shadow: 0 20px 40px rgba(0, 0, 0, 0.12);
  border-color: #cbd5e1;
}

.imageContainer {
  position: relative;
  height: 220px;
  overflow: hidden;
  background: linear-gradient(135deg, #f3f4f6 0%, #e5e7eb 100%);
}

.featuredImage {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: transform 0.6s cubic-bezier(0.4, 0, 0.2, 1);
}

.newsCard:hover .featuredImage {
  transform: scale(1.1);
}

.featuredBadge {
  position: absolute;
  top: 1rem;
  right: 1rem;
  background: linear-gradient(135deg, #f97316 0%, #ea580c 100%);
  color: white;
  padding: 0.375rem 0.875rem;
  border-radius: 24px;
  font-size: 0.75rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  box-shadow: 0 4px 12px rgba(249, 115, 22, 0.3);
  backdrop-filter: blur(8px)rem;
}

@media (max-width: 768px) {
  .articlesGrid {
    grid-template-columns: 1fr;
  }75rem;
  display: flex;
  flex-direction: column;
  flex-grow: 1;
}

.cardHeader {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 1.125rem;
  gap: 0.75rem;
}

.categoryTag {
  color: white;
  padding: 0.375rem 0.875rem;
  border-radius: 24px;
  font-size: 0.6875rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  white-space: nowrap;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.15);
}

.publishDate {
  font-size: 0.8125rem;
  color: #64748b;
  font-weight: 500;
}

.articleTitle {
  margin-bottom: 1rem;
  font-size: 1.3125rem;
  font-weight: 700;
  line-height: 1.35;
  letter-spacing: -0.01em;
}

.titleLink {
  color: #1e293b;
  text-decoration: none;
  transition: all 0.3s ease;
  display: inline-block;
}

.titleLink:hover {
  color: #3b82f6;
  transform: translateX(2px);
}

.excerpt {
  color: #64748b;
  line-height: 1.65;
  margin-bottom: 1.25rem;
  font-size: 0.937.25rem;
  align-items: center;
}

.tag {
  background: #f1f5f9;
  color: #475569;
  padding: 0.375rem 0.625rem;
  border-radius: 8px;
  font-size: 0.75rem;
  font-weight: 600;
  transition: all 0.2s ease;
}

.tag:hover {
  background: #e2e8f0;
  transform: translateY(-1px);
}

.moreTagsIndicator {
  color: #64748b;
  font-size: 0.75rem;
  font-style: italic;
  font-weight: 500;
}

.cardFooter {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding-top: 1.25rem;
  border-top: 1px solid #e5e7eb;
  margin-top: auto;
}

.authorInfo {
  font-size: 0.875rem;
  color: #64748b;
  font-weight: 500;
}

.readMoreBtn {
  color: #3b82f6;
  text-decoration: none;
  font-weight: 700;
  font-size: 0.875rem;
  transition: all 0.3s ease;
  display: inline-flex;
  align-items: center;
  gap: 0.25rem;
}

.readMoreBtn:hover {
  color: #1e40af;
  transform: translateX(4px)
.titleLink:hover {
  color: #3b82f6;
}

.excerpt {
  color: #64748b;
  line-height: 1.6;
  margin-bottom: 1rem;
  display: -webkit-box;
  -webkit-line-clamp: 3;
  line-clamp: 3;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

.tags {
  display: flex;
  flex-wrap: wrap;
  gap: 0.5rem;
  margin-bottom: 1rem;
  align-items: center;
}

.tag {75rem;
  margin: 3rem 0 2rem;
  flex-wrap: wrap;
}

.paginationBtn {
  padding: 0.75rem 1.25rem;
  border: 2px solid #e5e7eb;
  background: white;
  color: #374151;
  border-radius: 12px;
  cursor: pointer;
  transition: all 0.3s ease;
  font-weight: 600;
  font-size: 0.9375rem;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.04);
}

.paginationBtn:hover:not(:disabled) {
  border-color: #3b82f6;
  color: #3b82f6;
  transform: translateY(-2px);
  box-shadow: 0 4px 12px rgba(59, 130, 246, 0.15);
}

.paginationBtn:active:not(:disabled) {
  transform: translateY(0);
}

.paginationBtn:disabled {
  opacity: 0.4;
  cursor: not-allowed;
}

.pageNumbers {
  display: flex;
  gap: 0.5rem;
}

.pageNumber {
  padding: 0.75rem 1rem;
  border: 2px solid #e5e7eb;
  background: white;
  color: #374151;
  border-radius: 12px;
  cursor: pointer;
  transition: all 0.3s ease;
  font-weight: 600;
  min-width: 44px;
  text-align: center;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.04);
}

.pageNumber:hover {
  border-color: #3b82f6;
  color: #3b82f6;
  transform: translateY(-2px);
  box-shadow: 0 4px 12px rgba(59, 130, 246, 0.15);
}

.pageNumber:active {
  transform: translateY(0);
}
4rem 2rem;
  animation: fadeIn 0.3s ease;
}

/* Skeleton Loader */
.skeletonCard {
  background: white;
  border-radius: 20px;
  overflow: hidden;
  border: 1px solid #e5e7eb;
  box-shadow: 0 4px 20px rgba(0,0,0,0.06);
  animation: fadeIn 0.3s ease;
  height: 100%;
}

.skeletonImage {
  height: 220px;
  background: linear-gradient(90deg, #f9fafb 25%, #f3f4f6 37%, #f9fafb 63%);
  background-size: 400% 100%;
  animation: shimmer 1.4s ease-in-out infinite;
}

.skeletonContent {
  padding: 1.75rem;
}

.skeletonLine {
  height: 14px;
  border-radius: 8px;
  margin-bottom: 12px;
  background: linear-gradient(90deg, #f9fafb 25%, #f3f4f6 37%, #f9fafb 63%);
  background-size: 400% 100%;
  animation: shimmer 1.4s ease-in-out infinite;
}

.skeletonTags {
  height: 28px;
  border-radius: 12px;
  width: 45%;
  background: linear-gradient(90deg, #f9fafb 25%, #f3f4f6 37%, #f9fafb 63%);
  background-size: 400% 100%;
  animation: shimmer 1.4s ease-in-out infinite;
  margin-top: 1rem;
}

@keyframes shimmer {
  0% { background-position: 200% 0; }
  100% { background-position: -200% 0; }
}

.spinner {
  display: inline-block;
  width: 48px;
  height: 48px;
  border: 4px solid #e5e7eb;
  border-radius: 50%;
  border-top-color: #3b82f6;
  animation: spin 0.8s linear infinite;
  margin-bottom: 1.5rem;
}

@keyframes spin {
  to {
    transform: rotate(360deg);
  }
}

.loadingState p {
  color: #64748b;
  font-size: 1.125rem;
  font-weight: 500;
}

.errorState {
  text-align: center;
  padding: 4rem 2rem;
  animation: fadeIn 0.3s ease;
}

.errorState h2 {
  color: #ef4444;
  margin-bottom: 1rem;
  font-size: 1.75rem;
  font-weight: 700;
}

.errorState p {
  color: #64748b;
  margin-bottom: 2rem;
  font-size: 1.0625rem;
}

.retryBtn {
  padding: 0.875rem 1.75rem;
  background: linear-gradient(135deg, #3b82f6 0%, #2563eb 100%);
  color: white;
  border: none;
  border-radius: 12px;
  cursor: pointer;
  font-weight: 700;
  font-size: 1rem;
  transition: all 0.3s ease;
  box-shadow: 0 4px 12px rgba(59, 130, 246, 0.2);
}

.retryBtn:hover {
  transform: translateY(-2px);
  box-shadow: 0 6px 20px rgba(59, 130, 246, 0.3);
}

.retryBtn:active {
  transform: translateY(0);
}

.emptyState {
  text-align: center;
  padding: 4rem 2rem;
  animation: fadeIn968px) {
  .container {
    padding: 0 1rem;
  }
  
  .heroSection {
    padding: 3rem 1.75rem;
    margin-top: 1.5rem;
    border-radius: 20px;
  }
  
  .filtersSection {
    padding: 1.5rem;
  }
  
  .searchContainer {
    max-width: 100%;
  }
  
  .filterControls {
    width: 100%;
    flex-direction: column;
    align-items: stretch;
  }
  
  .categorySelect,
  .featuredFilter {
    width: 100%;
    justify-content: space-between;
  }
  
  .clearFiltersBtn {
    width: 100%;
  }
}

@media (max-width: 640px) {
  .container {
    padding: 0 1rem;
  }
  
  .heroSection {
    padding: 2.5rem 1.5rem;
    margin-top: 1rem;
    border-radius: 16px;
  }
  
  .featuredOverlay {
    padding: 1.5rem 1.25rem 1.25rem;
  }
  
  .filtersSection {
    padding: 1.25rem;
  }
  
  .cardContent {
    padding: 1.5rem;
  }
  
  .articleTitle {
    font-size: 1.1875rem;
  }
  
  .excerpt {
    font-size: 0.875rem;
  }
  
  .imageContainer {
    height: 200px;
  }
  
  .pagination {
    gap: 0.5rem;
  }Accessibility & Performance */
@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}

/* Focus States for Accessibility */
.searchInput:focus-visible,
.categorySelect:focus-visible,
.clearFiltersBtn:focus-visible,
.paginationBtn:focus-visible,
.pageNumber:focus-visible,
.retryBtn:focus-visible {
  outline: 3px solid #3b82f6;
  outline-offset: 2px;
}

.titleLink:focus-visible,
.readMoreBtn:focus-visible,
.featuredTitleLink:focus-visible {
  outline: 2px solid #3b82f6;
  outline-offset: 3px;
  border-radius: 4px;

    width: 100%;
    justify-content: center;
    margin-bottom: 0.75rem;
  }
  
  .resultsInfo {
    font-size: 0.875rem;
  }
}

@media (max-width: 480px) {
  .heroSection {
    padding: 2rem 1.25rem;
  }
  
  .featuredLabel {
    font-size: 0.75rem;
    padding: 0.3125rem 0.75rem;
  }
  
  .cardContent {
    padding: 1.25rem;
  }
  
  .cardHeader {
    flex-direction: column;
    align-items: flex-start;
    gap

.retryBtn:hover {
  background: #1e40af;
}

.emptyState {
  text-align: center;
  padding: 3rem;
}

.emptyState h3 {
  color: #374151;
  margin-bottom: 1rem;
}

.emptyState p {
  color: #64748b;
}

/* Responsive Design */
@media (max-width: 768px) {
  .container {
    padding: 0 0.5rem;
  }
  
  .heroSection {
    padding: 2rem 1rem;
    margin-top: 1rem;
  }
  
  .pageTitle {
    font-size: 2rem;
  }
  
  .pageSubtitle {
    font-size: 1rem;
  }
  
  .featuredCard {
    height: 250px;
  }
  
  .featuredCard.mainFeatured {
    height: 300px;
  }
  
  .featuredTitle {
    font-size: 1.25rem;
  }
  
  .mainFeatured .featuredTitle {
    font-size: 1.5rem;
  }
  
  .filtersSection {
    padding: 1rem;
  }
  
  .filterControls {
    justify-content: flex-start;
  }
  
  .categorySelect {
    min-width: 140px;
  }
  
  .cardContent {
    padding: 1rem;
  }
  
  .pagination {
    flex-wrap: wrap;
    gap: 0.25rem;
  }
  
  .pageNumbers {
    order: -1;
    width: 100%;
    justify-content: center;
    margin-bottom: 0.5rem;
  }
}

/* --- Modern Enhancements (appended) --- */
.heroSection { position: relative; overflow: hidden; box-shadow: 0 12px 30px rgba(30,58,138,0.18); backdrop-filter: saturate(120%) blur(4px); }
.featuredImage { transition: transform 0.35s ease; }
.featuredCard:hover .featuredImage { transform: scale(1.06); }
.featuredOverlay { animation: fadeInUp 0.4s ease; }
.articlesSection { animation: fadeIn 0.35s ease; }
.newsCard { animation: fadeInUp 0.35s ease; }
.titleLink { transition: color 0.2s, text-shadow 0.25s ease; }
.titleLink:hover { text-shadow: 0 2px 10px rgba(59,130,246,0.25); }
.paginationBtn:hover:not(:disabled) { transform: translateY(-1px); box-shadow: 0 6px 14px rgba(59,130,246,0.12); }
.pageNumber:hover { transform: translateY(-1px); }

@keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }
@keyframes fadeInUp { from { opacity: 0; transform: translateY(8px); } to { opacity: 1; transform: translateY(0); } }
@media (prefers-reduced-motion: reduce) { * { animation: none !important; transition: none !important; } }
