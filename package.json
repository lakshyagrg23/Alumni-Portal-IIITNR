{
  "name": "iiit-nr-alumni-portal",
  "version": "1.0.0",
  "description": "IIIT Naya Raipur Alumni Portal - Full Stack Application",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "test": "npm run test --prefix backend",
    "lint": "npm run lint --prefix backend && npm run lint --prefix frontend",
    "lint:fix": "npm run lint:fix --prefix backend && npm run lint:fix --prefix frontend",
    "setup": "npm run install:all",
    "clean": "rm -rf node_modules frontend/node_modules backend/node_modules",
    "postinstall": "echo 'Installation complete! Run npm run dev to start development servers.'"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": [
    "alumni",
    "portal",
    "iiit",
    "naya-raipur",
    "react",
    "nodejs",
    "express",
    "postgresql"
  ],
  "author": "IIIT Naya Raipur",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/lakshyagrg23/Alumni-Portal-IIITNR.git"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "dependencies": {
    "json2csv": "^6.0.0-alpha.2"
  }
}
